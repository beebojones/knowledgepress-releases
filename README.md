# KnowledgePress

Signed release builds of KnowledgePress, a small Windows desktop utility.

Each release includes `KnowledgePress.zip`, a SHA-256 checksum (`.sha256`), and an Ed25519 signature (`.sig`). The app verifies both before installing any update.
